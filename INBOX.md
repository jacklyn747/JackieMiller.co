# Inbox — routed items awaiting a JackieMiller.co session


## Drained 2026-07-15 (this session)

- ~~Dark mode mobile QA~~ — done. Field Notes pages are solid on mobile dark; one real defect found and fixed on the branch before merge: dark `--ox` (#4A1210) is a fine surface color but illegible (~1.2:1) as small text — dates/links/blockquote rules now use new `--ox-accent` (#B04A44 dark, unchanged in light). Homepage findings folded into the Dreamscape item above. Note for future browser QA: the preview pane freezes animation timelines (`visibilityState: hidden`), so entrance-animated content screenshots as blank — that's the tool, not the site.
- ~~Rebase PR #2 onto new main~~ — done and merged (squash, `6031867`). Conflicts were package.json/lockfile only (both branches added gsap; kept gsap + marked); globals.css auto-merged cleanly.
- ~~Decide PR #1~~ — **merged** (squash, `e690d46`): the positioning + system-architecture docs are the canon site copy will be written against, and it merged clean. The in-repo end-of-day skill and its setup prompt were removed in a follow-up commit (`6683178`) — superseded by the matured Claude-Ops global copy.
- 2026-07-15: ~~ROOT CAUSE of "my site is a 404"~~ **RESOLVED 2026-07-15 5:30 PM — SITE IS LIVE.** Two stacked June-26 setup defects: (1) domain attached to empty project `project-q17k0` — fixed by project deletions + domain remove/re-add onto `jackie-miller-co`; (2) **Framework Preset was "Other"** — Vercel built Next.js perfectly on every push but served none of it (every deployment 404'd at its own URL, since the project's creation). Fixed: preset → Next.js, redeployed, verified live: /, /field-notes, /field-notes/the-four-layers all 200 on jackiemiller.co. **Standing gate amendment: every deploy verification ends by fetching the real domain a human types — never trust a green build.**
- 2026-07-15: **FULL GROUND-UP REBUILD — THE next action (supersedes all earlier homepage framing).** Jacklyn's decision: stop patching the broken current site, rebuild JackieMiller.co from scratch as the immersive dreamscape. Governing doc: [docs/brief-dreamscape-homepage.md](docs/brief-dreamscape-homepage.md) — now headed by a scope-change banner with the mandatory deploy-verify gate (load the real jackiemiller.co in a real browser before calling anything done — the in-app preview pane LIES by freezing animations). Carry forward only the Field Notes posts. Start a fresh JackieMiller.co session → NanoBanana render iteration with Jacklyn → build under Tier-2 rules.
